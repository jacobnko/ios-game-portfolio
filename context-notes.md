# context-notes.md

작업 중 내린 결정과 그 이유를 계속 append한다.
다음 세션(사람이든 에이전트든)이 결정을 다시 유도하지 않아도 되게 하는 것이 목적이다.

---

## 2026-09-14 · S0.1 문서 뼈대

### D-001. 저장소는 모노레포, 게임은 독립 Xcode 프로젝트
- **결정.** `Packages/CoreKit` 하나 + `Apps/<Game>` 다수를 한 저장소에 둔다. 게임끼리는 의존하지 않는다.
- **이유.** `CoreKit`을 고치면 전 게임이 같은 커밋에서 함께 검증된다. 게임별 저장소로 쪼개면 버전 핀 관리 비용이 솔로 개발자에게 과하다.
- **대가.** 저장소가 커진다. 게임이 10개를 넘어가면 재검토한다.

### D-002. `CoreKit`을 3개 타겟으로 분리
- **결정.** `CoreKitJuice`(외부 의존 0) / `CoreKitData`(외부 의존 0) / `CoreKitServices`(AdMob · StoreKit · Firebase).
- **이유.** 광고/분석 SDK를 juice·데이터 레이어에 섞으면 SwiftUI Preview가 느려지고 유닛 테스트가 SDK를 요구하게 된다. juice는 반복해서 미세 조정할 코드라 Preview 속도가 곧 개발 속도다.
- **대가.** 타겟 3개를 관리해야 한다. 단일 타겟보다 초기 설정이 조금 더 든다.

### D-003. 주석과 `CLAUDE.md`는 영어, 작업 문서는 한국어
- **결정.** 코드 주석(파일 헤더 포함)과 `CLAUDE.md`는 영어. `PLAN.md` · `checklist.md` · `context-notes.md` · 디자인 핸드오프는 한국어.
- **이유.** 전역 규칙의 "한국어 파일 헤더 주석"과 프로젝트 규칙의 "주석 전부 영어"가 충돌했다. 공개 포트폴리오 코드라는 점을 근거로 영어를 택했다.
- **되돌리는 법.** `CLAUDE.md` §1의 해당 항목만 바꾸면 된다. 기존 파일은 점진적으로 정리한다.

### D-004. 디자인은 Claude Design으로 opt-out, 핸드오프 문서로만 연결
- **결정.** Claude Code는 에셋을 만들지 않는다. `docs/design/`의 D1~D6 카드를 스텝별 분할 실행한다.
- **이유.** Claude Design은 토큰 소모가 커서 한 세션에 게임 전체를 넘기면 컨텍스트가 터진다. 또한 아트 방향 결정은 사람이 중간에 개입해야 품질이 나온다.
- **운영 규칙.** 한 세션 = 한 카드. 이전 산출물은 원본이 아니라 **텍스트로 요약한 토큰(hex 색상, 폰트명, 키워드)** 만 다음 카드에 넘긴다.

---

## Open Questions — S0.3 착수 전에 답이 필요

### Q1. `CoreKitServices`가 Firebase까지 포함할 것인가
- 광고·결제는 전 게임 공통이 확실하다. 분석은 Firebase 대신 앱 타겟에서 주입하는 방식도 가능하다.
- **C의 제안.** 프로토콜은 `CoreKitServices`에 두고 Firebase 구현체도 같은 타겟에 둔다. 게임 10개가 전부 Firebase를 쓸 것이므로 추상화만 남기는 건 과설계다.
- **상태.** 미확정.

### Q2. 최소 지원 iOS 버전
- SwiftData + CloudKit은 iOS 17+, 일부 SwiftUI API와 String Catalog 개선은 iOS 18+에서 편하다.
- **C의 제안.** **iOS 18.0**. 캐주얼 게임 타깃층의 OS 업데이트율이 높고, 하위 호환 분기 비용이 신규 코드 속도를 깎는다.
- **상태.** 미확정.

### Q3. 번들 ID 규칙
- **확정.** `com.jacobkostudio.<codename>` / IAP `com.jacobkostudio.<codename>.removeads` (D-009).

### Q4. 원격 저장소 공개 여부
- 포트폴리오 목적이면 public이 유리하나, 광고 ID·번들 설정·퍼즐 데이터가 노출된다.
- **C의 제안.** private으로 시작하고, 첫 출시 후 `CoreKit`만 별도 public 저장소로 떼어낸다.
- **상태.** 미확정.

### Q5. 게임 #1 스테이지 데이터 생성 방식
- 손으로 만든 퍼즐 30개 vs 생성기 + 검증기.
- **C의 제안.** 검증기를 먼저 만들고(자동), 퍼즐 자체는 초반 30개만 수작업으로 난이도 곡선을 잡는다. 생성기는 게임 #1이 반응을 얻은 뒤에 만든다.
- **상태.** 미확정. Phase 3 진입 전까지만 답하면 된다.

---

## 2026-09-14 · S0.2 저장소 초기화

### D-005. Open Question Q1~Q4 확정
| | 질문 | 결정 | 이유 |
|---|---|---|---|
| Q1 | `CoreKitServices`에 Firebase 구현체 포함 | **포함한다** | 게임 10개가 전부 Firebase를 쓴다. 프로토콜만 남기는 건 과설계다 |
| Q2 | 최소 지원 iOS | **iOS 18.0** | 하위 호환 분기 비용이 신규 코드 속도를 깎는다. 캐주얼 타깃층의 OS 업데이트율이 높다 |
| Q3 | 번들 ID 규칙 | **`com.jacobkostudio.<codename>`** / IAP `com.jacobkostudio.<codename>.removeads` | 개인 앱이 아니라 스튜디오 포트폴리오라는 신호를 번들 ID 레벨에서 준다. **코드네임 기준이며 스토어 표시명과 무관하다** |
| Q4 | 원격 저장소 | **private으로 시작** | 첫 출시 후 `CoreKit`만 별도 public 저장소로 분리한다 |

### D-006. 게임 이름은 코드네임과 스토어 이름을 분리한다
- **결정.** 코드네임(폴더·타겟·번들 ID)은 Xcode 프로젝트 생성 시점에 고정하고, App Store 표시명은 출시 3주 전에 확정한다. 둘은 일치할 필요가 없다.
- **이유.** 번들 ID는 사실상 되돌릴 수 없지만 표시명은 새 버전과 함께 바꿀 수 있다. 이름 결정이 개발 착수를 막을 이유가 없다.
- **상세.** `docs/architecture/naming.md`

### D-007. 상표 위험 때문에 이름 후보를 사전 검증한다
- **결정.** 초기 작업명 일부가 타사 등록상표와 충돌해 전부 조어로 교체했다. 확정된 브랜드명은 `docs/concepts/game-concepts.md`(비공개)에서 관리한다.
- **이유.** 널리 알려진 퍼즐 장르명 중 일부는 일반명사가 아니라 특정 회사의 등록상표다. 또 과거 Apple이 특정 단어가 포함된 앱을 대량 리젝한 전례가 있다.
- **시점 근거.** 번들 ID 확정 후에 바꾸면 프로젝트 전체를 따라가야 한다. 사전 검증이 가장 싸다.


### D-008. 이름 중복 확인은 스크립트로 자동화한다
- **결정.** `scripts/check-name.sh <name> [country]` — iTunes Search API로 us/kr/jp 중복을 확인한다.
- **검증.** "Pipely"로 실행했더니 "Pipely - Flow Connect"(Souplin Labs)가 이미 존재했다. 스크립트가 실제로 작동한다.
- **한계.** App Store 등재 여부만 본다. **상표 검색(USPTO/KIPRIS)은 별도로 해야 한다.**

### D-009. 번들 ID 프리픽스를 `com.jacobkostudio`로 확정
- **결정.** `com.jacobkostudio.<codename>`, IAP는 `com.jacobkostudio.<codename>.removeads`.
- **이유.** 게임 10종을 묶는 포트폴리오이므로 개인 이름보다 스튜디오 네임스페이스가 맞다. 기존 `jacobko.app` 포트폴리오 앱들과도 구분된다.
- **주의.** 번들 ID는 App Store Connect에 한 번 등록되면 **삭제도 재사용도 불가능하다.** 오타 하나가 영구히 남으므로 첫 앱 등록 때 프리픽스 철자를 반드시 재확인한다.
- **확인 필요.** Apple Developer 포털의 App ID prefix(Team ID)는 그대로 두고, Bundle ID 문자열만 이 규칙을 쓴다.

### D-010. 커밋은 Claude가 자동으로 한다. AI attribution 트레일러는 붙이지 않는다
- **결정.** 스텝이 끝나면 Claude가 직접 `git add` + `git commit`까지 수행한다. 사용자에게 복붙용 명령을 넘기지 않는다. 커밋 메시지에 `Co-Authored-By` 등 AI 흔적을 남기지 않는다.
- **이유.** 전역 수정이 잦은 문서 작업 단계에서 수동 커밋은 순수 마찰이다. 또한 이 저장소는 공개 포트폴리오로 전환될 예정이라 커밋 로그가 사람의 작업 기록으로 읽혀야 한다.
- **경계.** `git push`는 자동화하지 않는다. 커밋은 되돌릴 수 있지만 push는 외부로 나가는 행위다.

### D-011. 저장소는 공용 1개 + 게임별 N개로 분리한다
- **결정.** `github.com/jacobnko/ios-game-portfolio` (public)에는 `CoreKit`·문서·디자인 템플릿만 둔다. 게임은 각자 별도 저장소이고, 로컬에서 `Apps/<Codename>/`에 클론되며 이 저장소의 `.gitignore`가 `Apps/*`를 막는다.
- **이유.** 게임별 히스토리·이슈·릴리즈 태그가 독립적이고, 게임마다 공개 여부를 따로 정할 수 있다. 저장소가 10개 게임 에셋으로 비대해지지 않는다.
- **대가.** `CoreKit` 수정이 게임 저장소에 자동 전파되지 않는다. 지금은 local package 경로 참조라 디스크 상으로는 즉시 반영되지만 CI에서는 성립하지 않는다.
- **전환 신호.** "CoreKit을 고쳤는데 어느 게임이 깨졌는지 모르겠다"는 상황이 처음 오면 원격 패키지 + 버전 태그로 전환한다. 게임 3개 근처로 예상한다.
- **함정.** 새 게임 저장소는 이 저장소의 `.gitignore` 보호를 못 받는다. 생성 즉시 시크릿 패턴을 복사해야 한다.

### D-012. 공용 저장소는 public, push는 Claude가 자동 수행
- **결정.** J가 push 권한을 위임했다. 커밋 직후 자동 push한다.
- **감수하는 리스크.** `PLAN.md`에 10개 게임 컨셉과 도파민 트위스트가 공개된다. 캐주얼 퍼즐은 클론 속도가 빠른 장르라 로드맵이 노출된다. J가 포트폴리오 노출 이득을 우선해 선택했다.
- **강제 규칙.** public이므로 push 전 시크릿 스캔이 필수다. 한 번 push된 시크릿은 삭제가 아니라 **폐기(revoke)** 해야 한다. Phase 1의 AdMob·Firebase 연동 구간이 실제 위험 지점이다.

---

## 2026-09-14 · 게임 컨셉·브랜드 확정

### D-013. 10개 게임의 브랜드명을 사전 확정하고 실제로 검증했다
- **결정.** 전 게임의 브랜드명·부제·세계관·조작·도파민 트위스트를 개발 착수 전에 확정했다. 문서는 `docs/concepts/game-concepts.md`(비공개).
- **검증 방법.** iTunes Search API로 후보 40개를 미국 스토어에서 1차 필터링하고, 최종 10개를 미국·한국·일본 3개 스토어에서 재확인했다. 동일 이름 앱이 있는 후보 4개와 유사 앱이 있는 후보 2개를 탈락시켰다.
- **이유.** 이름은 번들 ID와 묶여서 나중에 바꾸는 비용이 크다. 또 10개를 한 번에 놓고 봐야 팔레트·톤이 겹치는지 판단할 수 있다 — Guideline 4.3 대응은 게임 하나씩 볼 때는 불가능하다.
- **제약.** 브랜드명을 전부 10자 이하로 잡았다. 홈 화면 아이콘 아래는 약 12자에서 잘린다.
- **남은 일.** App Store Connect 예약 직전 USPTO·KIPRIS 상표 검색. App Store 등재 검색만으로는 상표 확인이 되지 않는다.

### D-014. 개발 순서를 바꿨다 — SpriteKit을 2번째로 앞당긴다
- **결정.** `Chordline`(SwiftUI) → `Noodlewing`(SpriteKit) → `Blobstack`(SpriteKit) → `Pixlaugh`(SwiftUI) 순.
- **이유.** 원안은 퍼즐 5개를 끝내고 액션으로 넘어가는 순서였다. 그러면 `CoreKit`이 SpriteKit을 견디는지가 5번째 게임에서야 드러난다. 구조 리스크는 일찍 터뜨려야 싸다.
- **부수 효과.** `Noodlewing`은 바이럴 가능성이 가장 높은 게임이라 초반 트래픽 확보에도 유리하다.

### D-015. 공개 저장소에서 게임 컨셉·브랜드명을 전부 제거했다
- **결정.** `PLAN.md`와 `docs/concepts/`를 gitignore하고, 추적 중이던 문서에서 게임명을 익명화했다 (`<Codename>`, "게임 #1" 등).
- **이유.** 캐주얼 퍼즐은 클론 속도가 빠른 장르다. 출시 전 로드맵과 도파민 트위스트가 공개되면 그대로 베껴진다.
- **한계 — 중요.** `PLAN.md`는 **이미 push된 커밋 히스토리에 남아 있다.** 현재 HEAD에서는 사라졌지만 과거 커밋을 열면 보인다. 완전 제거는 히스토리 재작성 + force push가 필요하며 아직 수행하지 않았다.

---

## 2026-09-14 · S0.3 CoreKit 스캐폴드

### D-016. 패키지에 macOS 플랫폼을 같이 선언한다
- **결정.** `platforms: [.iOS(.v18), .macOS(.v15)]`. 플랫폼 전용 코드는 `#if canImport(...)`로 감싼다.
- **이유.** iOS만 선언하면 `swift build` / `swift test`를 CLI에서 돌릴 수 없고 시뮬레이터가 필요해진다. 퍼즐 검증기·데이터 모델 같은 순수 로직 테스트는 밀리초 단위로 돌아야 개발 속도가 산다. 실측 `swift test` 3.5초.
- **대가.** `CoreKitJuice`에 `CoreHaptics`/`UIKit`이 들어오면 macOS 빌드가 깨진다. 그때마다 `canImport` 가드를 붙여야 한다. 이 비용을 감수한다.

### D-017. 스캐폴드 단계에서 외부 의존성을 넣지 않는다
- **결정.** `Package.swift`의 `dependencies`는 비워 둔다. AdMob은 S1.6, Firebase는 S1.7에서 `CoreKitServices`에만 붙인다.
- **이유.** 지금 넣으면 빌드마다 원격 패키지를 해석해야 하고 오프라인 빌드가 막힌다. 스캐폴드의 목적은 골격 검증이지 의존성 검증이 아니다.

### D-018. `allowsProductionAdUnits`를 기본 `false`로 잠근다
- **결정.** `CoreKitServices.allowsProductionAdUnits`는 DEBUG에서 항상 `false`, RELEASE에서도 환경변수 `COREKIT_PRODUCTION_ADS=1`이 있어야 `true`.
- **이유.** 실제 광고 유닛을 개발 중에 호출하면 무효 트래픽이고 계정 정지 사유다. 기본값을 안전한 쪽으로 잠가서 **실수의 방향을 한쪽으로만** 만든다.

### D-019. 배경음은 질감이고 효과음이 음악이다
- **결정.** BGM은 멜로디 없는 앰비언트로 깔고 볼륨을 낮게(0.35) 둔다. juice 효과음 재생 시 -6dB 덕킹. `AVAudioSession`은 `.ambient` + `.mixWithOthers`.
- **이유.** 이 포트폴리오는 피치 상승 효과음이 게임 메커닉 자체다. 멜로디가 뚜렷한 BGM은 그 도파민 트리거와 정면으로 싸운다. 또 캐주얼 게임 사용자는 자기 음악을 틀어 놓는 경우가 많아 기본 오디오 세션은 그걸 정지시킨다.
- **조달.** BGM은 FreePD(퍼블릭 도메인, 크레딧 불필요), SFX는 Kenney(CC0). 게임이 10개라 CC-BY의 크레딧 유지 비용을 피한다.
- **한계.** Claude는 오디오를 들을 수 없다. 곡 선택은 J가 해야 하고, Claude는 검색 조건 정의와 다운로드·변환 자동화를 맡는다.

---

## 2026-09-14 · S1.1 Haptics

### D-020. BGM은 게임 #1 완성 후로 미룬다
- **결정.** 배경음 도입 여부를 게임 #1이 플레이 가능한 상태가 된 뒤에 판단한다. 지금은 `AVAudioSession` 구성과 볼륨 채널 분리만 해두고 BGM을 끼울 자리만 남긴다.
- **이유.** 효과음이 이 게임들의 메커닉이라 BGM이 필요한지 자체가 실제로 플레이해 봐야 안다. 곡 선택은 Claude가 못 하는 작업(청취 평가)이라 J의 시간을 쓰는데, 필요 없을 수도 있는 일에 먼저 쓰지 않는다.
- **참조.** 사양과 조달 전략은 `docs/architecture/audio-assets.md`에 이미 정리되어 있다. 필요해지면 그때 꺼내 쓴다.

### D-021. 햅틱을 순수 매핑(`HapticRecipe`)과 플랫폼 계층(`HapticEngine`)으로 쪼갠다
- **결정.** intent → (intensity, sharpness, eventCount, spacing) 변환은 `Foundation`만 쓰는 순수 구조체로 분리하고, CoreHaptics는 얇은 재생 계층에만 둔다.
- **이유.** "어떤 느낌이어야 하는가"가 테스트할 가치가 있는 부분이고, 그건 기기 없이 밀리초 안에 검증할 수 있어야 한다. 실제로 테스트 10종이 0.001초에 돈다.
- **보호하는 규칙 2개.** `micro`의 최대 강도는 `milestone`보다 항상 낮다 (진행과 완료를 손으로 구분). `error`는 `milestone`보다 항상 날카롭다 (성공과 실패를 손으로 구분). 둘 다 테스트로 고정했다.

### D-022. CoreHaptics 엔진 중단·리셋 핸들러를 처음부터 넣는다
- **결정.** `stoppedHandler`와 `resetHandler`에서 엔진을 버리고 다음 재생 때 다시 만든다.
- **이유.** 시스템은 백그라운드 전환·오디오 인터럽션·발열 시 엔진을 멈춘다. 핸들러가 없으면 **그 세션 내내 햅틱이 조용히 죽어 있는다.** CoreHaptics에서 가장 흔한 버그이고, 증상이 "가끔 진동이 안 온다"라서 디버깅이 어렵다.
- **추가 안전장치.** 재생 실패 시 예외를 삼키고 `UIImpactFeedbackGenerator` 폴백으로 떨어진다. 햅틱 하나 때문에 프레임을 실패시키지 않는다.

### D-023. macOS 빌드만으로는 검증이 끝나지 않는다
- **문제.** `swift test`는 호스트(macOS)에서 돌기 때문에 `#if canImport(UIKit)` 안쪽 코드가 **아예 컴파일되지 않는다.** 테스트가 통과해도 iOS 코드가 깨져 있을 수 있다.
- **대응.** `scripts/verify.sh`가 host 테스트와 `xcodebuild -destination 'generic/platform=iOS'` 빌드를 함께 돌린다. **커밋 전 필수.**

### D-024. 실기기 체감 검증용 하네스 앱 `Tools/JuiceLab`을 만든다
- **문제.** `CoreKit`은 패키지라서 기기에 설치할 대상이 없다. 그런데 **햅틱은 시뮬레이터에서 아예 발생하지 않는다.** 빌드가 통과해도 실제로 어떻게 느껴지는지는 확인할 방법이 없었다.
- **결정.** `Tools/JuiceLab`에 XcodeGen 기반 하네스 앱을 둔다. 게임이 아니고 출시 대상도 아니다. 번들 ID `com.jacobkostudio.juicelab`.
- **확장.** S1.2 오디오 피치 상승, S1.3 승리 연출 타이밍도 전부 여기서 먼저 검증하고 게임에 넣는다. 한 번 만들어 두면 계속 쓴다.
- **XcodeGen을 쓰는 이유.** `.xcodeproj`는 diff가 불가능하고 충돌이 잦다. `project.yml`만 추적하고 프로젝트는 생성물로 취급한다 (`.gitignore`에 `Tools/*/*.xcodeproj`).
- **`verify.sh`에 3단계로 편입.** host 테스트 → CoreKit iOS 빌드 → JuiceLab iOS 빌드.

### D-025. "빌드된다"와 "동작한다"를 구분해서 보고한다
- **결정.** 사람이 느끼거나 봐야만 검증되는 것(햅틱, 소리, 애니메이션 타이밍)은 Claude가 완료로 보고하지 않는다. JuiceLab에 넣고 **무엇을 느껴야 하는지 구체적인 체크리스트**를 J에게 넘긴다.
- **이유.** 햅틱은 시뮬레이터에서 발생하지 않으므로 CI나 자동 검증으로 대체할 수 없는 영역이 존재한다. 이걸 흐리면 "통과"가 의미를 잃는다.
- **CLAUDE.md §2에 규칙으로 추가했다.**

---

## 2026-09-14 · S1.2 Audio

### D-026. 효과음은 샘플 피치 시프트가 아니라 코드 합성으로 만든다
- **결정.** 짧은 톤을 `AVAudioEngine`용 PCM 버퍼로 직접 렌더링한다. 사인파 + 3배음 18%에 지수 감쇠 엔벨로프.
- **이유.** 원래 계획은 Kenney 원샷 샘플을 피치 시프트하는 것이었는데, 합성이 모든 면에서 낫다.
  - 음정이 **정확하다.** 샘플을 시프트하면 포먼트가 같이 밀려서 높은 음이 부자연스러워진다.
  - **번들 용량 0, 라이선스 의무 0.** 게임 10개에 걸쳐 누적되는 비용이다.
  - **에셋 확보를 기다릴 필요가 없다.** S1.2가 외부 다운로드에 막히지 않는다.
- **대가.** 합성음은 잘못 만들면 "삐" 소리로 들린다. 3배음을 섞어 말렛 타격음에 가깝게 잡았고, 실기기 청취로 확정해야 한다.
- **여지.** `ToneRecipe`가 순수 구조체라, 나중에 샘플 레이어를 덧씌우고 싶으면 재생 계층만 바꾸면 된다.

### D-027. 음계는 펜타토닉이 기본이다
- **결정.** `MusicalScale.pentatonicMajor` (반음 0,2,4,7,9)를 기본값으로 한다.
- **이유.** 펜타토닉은 **어느 음을 어떤 순서로 쳐도 불협이 나지 않는다.** 플레이어가 빠르게·불규칙하게 조작해도 결과가 의도된 것처럼 들린다. 메이저 스케일은 이걸 보장하지 못한다.
- **상한.** 2옥타브에서 상승을 멈춘다. 그 위는 날카로워서 보상으로 안 들린다. 테스트로 고정했다.

### D-028. 마일스톤은 높이가 아니라 무게로 구분한다
- **결정.** `milestone`은 루트 1옥타브 위로 **내려온 뒤** 길이와 음량을 키운다. micro 상승분보다 높게 가지 않는다.
- **이유.** micro가 2옥타브까지 올라가므로 마일스톤이 음정으로 이기려면 더 높아져야 하는데, 그 영역은 이미 날카롭다. 길이(0.35s vs 0.11s)와 게인(0.7 vs 0.45)으로 구분하는 편이 "도착했다"는 느낌에 맞는다.
- **기록 이유.** 테스트 이름을 `milestoneSitsAbove...`로 썼다가 단언은 `<`인 상태였다. 이름이 설계와 어긋나 있었고, 의도를 명시적으로 남겨둔다.

### D-029. 오디오 엔진도 재시작 핸들러가 필요하다
- **결정.** `.AVAudioEngineConfigurationChange` 관측 후 엔진을 재구성한다.
- **이유.** 이어폰 연결·해제 같은 라우트 변경이 엔진을 리셋한다. 처리하지 않으면 **그 세션 내내 소리가 안 난다.** D-022의 CoreHaptics `stoppedHandler` 문제와 같은 부류다.
- **재생 정책.** `scheduleBuffer` 시 `.interrupts`를 준다. 빠른 드래그에서 톤이 겹쳐 뭉개지지 않게 이전 음을 대체한다.

### D-030. `verify.sh`가 XcodeGen 재생성을 먼저 돌린다
- **문제.** JuiceLab에 새 파일을 추가했더니 `cannot find 'AudioLabView' in scope`로 빌드가 깨졌다. XcodeGen은 생성 시점의 파일 목록을 프로젝트에 박아 넣는다.
- **대응.** `verify.sh`가 빌드 전에 `xcodegen generate`를 항상 실행한다. 사람이 기억해야 하는 단계를 없앤다.

---

## 2026-09-14 · S1.3 Victory catharsis

### D-031. 승리 연출은 레이어를 의도적으로 어긋나게 배치한다
- **결정.** 흔들림 0.0~0.35s, 파티클 0.02~1.02s, 배수 표시 0.30~1.15s, 전체 1.25s.
- **이유.** 세 레이어를 같은 프레임에 터뜨리면 **한 번의 섬광**으로 읽힌다. 1초에 걸쳐 어긋나게 놓아야 플레이어가 각각을 인지하고, 그게 "따냈다"는 감각을 만든다.
- **끝의 정적.** 마지막 파티클이 죽자마자 다음 화면으로 넘기면 연출이 잘린 느낌이 난다. `total`에 여유 구간을 둔다. 테스트로 `total > burst.end`를 고정했다.

### D-032. 흔들림 곡선은 지수 감쇠가 아니라 2차 감쇠를 쓴다
- **결정.** `pow(1 - progress, 2)` 엔벨로프.
- **이유.** 지수 감쇠는 **절대 0에 도달하지 않는다.** 뷰가 0.3pt쯤 어긋난 채로 세션 내내 남는다. 2차 감쇠는 끝점에서 정확히 0이다. 테스트로 `offset(at: duration) == .zero`를 고정했다.
- **X·Y 분리.** 두 축에 같은 주파수를 쓰면 대각선 직선 운동이 되어 충격이 아니라 글리치로 보인다. Y축은 주파수 1.37배 + 위상 1.1 오프셋.

### D-033. 파티클 버스트는 시드 결정론적이다
- **결정.** SplitMix64로 시드에서 버스트를 생성한다. 같은 시드 → 같은 버스트.
- **이유.** 매번 다른 버스트는 **튜닝할 수도, 버그 리포트로 재현할 수도 없다.** 게임별로 시드만 바꾸면 서로 다른 느낌이 나오면서도 각자는 재현 가능하다.

### D-034. 테스트가 실제 버그를 잡았다 — 파티클이 아래로 발사되고 있었다
- **증상.** `angle = rng(-π...0) + rng(-0.35...0.35)`. 지터가 범위 양끝을 넘기면 각도가 수평선을 넘어가서 `sin(angle) > 0`, 즉 아래 방향 속도가 나온다.
- **영향.** 버스트 일부가 처음부터 아래로 흘러서 "터진다"가 아니라 "샌다"로 보인다.
- **수정.** 지터 후 각도를 `[-π+0.06, -0.06]`으로 클램프한다.
- **기록 이유.** 순수 함수로 분리해 두지 않았으면 이건 기기에서 눈으로 봐야만 발견됐을 버그다. 순수/플랫폼 분리의 실익이 실제로 나온 사례다.

### D-035. Reduce Motion에서도 보상은 준다
- **결정.** 접근성 Reduce Motion이 켜지면 화면 흔들림을 제거하고 파티클을 1/3로 줄인다. **연출 자체를 없애지는 않는다.**
- **이유.** 이 게임들의 보상 구조가 승리 연출에 걸려 있다. 연출을 통째로 건너뛰면 해당 사용자에게는 게임의 절반이 사라진다.

### D-036. 파티클은 Canvas 단일 패스로 그린다
- **결정.** 파티클마다 View를 만들지 않고 `Canvas` 하나에서 전부 그린다.
- **이유.** 파티클당 View를 만드는 방식은 수십 개만 넘어가도 프레임이 무너진다. 승리 연출은 기본 90개, 설정에 따라 300개까지 간다.

---

## 2026-09-14 · S1.4 Persistence

### D-037. 집계를 저장하지 않고 파생한다
- **결정.** 총 별·총 점수·클리어 수를 별도 모델로 저장하지 않고 `ProgressSummary`가 스테이지 기록에서 계산한다.
- **이유.** 집계를 저장하면 **진리가 두 개**가 되고, 둘이 독립적으로 동기화되면서 어긋난다. "별 12개인데 스테이지 합은 9개"가 나오는 순간 사용자는 데이터를 못 믿는다. 파생은 어긋날 수가 없다.
- **대가.** 스테이지가 수천 개가 되면 매번 계산이 부담이다. 게임당 30~100 스테이지 규모에서는 문제가 아니다.

### D-038. CloudKit에 유니크 제약이 없다는 것에 대한 실제 대응
- **문제.** `@Attribute(.unique)`를 쓸 수 없으므로 기기 두 대가 같은 스테이지에 각각 행을 만들면 **둘 다 살아남는다.** DB가 막아주지 않는다.
- **대응.** `ProgressStore`가 읽기·쓰기마다 같은 `stageId` 행을 전부 모아 병합하고 여분을 삭제한다. 게임은 `ModelContext`를 직접 만지지 않는다 — 이 복구가 한 곳에만 있어야 한다.
- **핵심 제약 — 병합은 멱등이어야 한다.** 동기화는 여러 패스에 걸쳐 수렴하고 그때마다 병합이 다시 돈다. 그래서 `attemptCount`는 합이 아니라 **최댓값**을 쓴다. 합으로 하면 재병합마다 부풀어 오른다.
- **테스트로 고정한 것.** 멱등성, 교환법칙, 그리고 "0은 미기록이므로 최단 시간 비교에서 이기면 안 된다".

### D-039. 광고 제거 상태는 여기 저장하지 않는다
- **재확인.** 진행도만 CloudKit에 둔다. 엔타이틀먼트의 유일한 진리는 `Transaction.currentEntitlements`다.
- **이유.** CloudKit 플래그를 믿으면 두 방향으로 깨진다. 동기화가 안 되는 사용자가 **구매한 광고 제거를 못 받고**, 반대로 데이터를 조작해 **결제 없이 광고를 제거**할 수 있다.
- 진행도는 유실돼도 게임이 계속되지만 엔타이틀먼트는 그렇지 않다.

### D-040. 출시 사고 1순위 — Production 스키마 배포
- **함정.** SwiftData가 개발 중 만드는 CloudKit 스키마는 **Development 환경에만** 존재하고 자동 승격되지 않는다. 그대로 출시하면 **실사용자에게만 동기화가 실패한다.** 개발자 기기에서는 멀쩡해 보인다.
- **대응.** `checklist.md`의 Phase 4 첫 항목으로 올렸다. 절차는 `docs/architecture/cloudkit-setup.md`.
- **추가 제약.** Production 스키마는 **추가만 가능**하다. 필드 이름 변경·삭제·타입 변경이 안 된다. 첫 출시 전에 스키마를 확정해야 하고, 이후 변경은 "새 optional 필드 추가"만 가능하다고 가정한다.

---

## 2026-09-14 · S1.5 StoreKit 2

### D-041. StoreKit 앞에 프로토콜 이음새를 둔다
- **결정.** `StoreClient` 프로토콜을 정의하고 `StoreKitClient`가 구현한다. `PurchaseManager`는 프로토콜에만 의존한다.
- **이유.** StoreKit은 패키지 테스트 타겟에서 구동할 수 없다(앱 호스트 + StoreKitTest 필요). 그런데 엔타이틀먼트 규칙은 **틀리면 돈이 걸리는** 코드다. 유료 사용자가 구매를 잃거나, 반대로 결제 없이 제품이 나간다.
- **근거가 된 결과.** 이 이음새 덕에 환불 후 광고 복귀, 미검증 거래 거부, `pending` 처리, 복원 실패 시 재조회까지 16종을 자동 테스트로 고정했다. 추상화를 위한 추상화가 아니다.

### D-042. `adsRemoved`는 성공 경로에서도 직접 세팅하지 않는다
- **결정.** 구매가 `.purchased`로 돌아와도 `adsRemoved = true`를 쓰지 않고 `currentEntitlements`를 다시 읽는다.
- **이유.** 진리를 한 곳으로 유지한다. 해피 패스에서만 예외를 두면 그 예외가 나중에 캐시로 자라난다.
- **같이 고정한 규칙.** `.unverified`는 아무것도 부여하지 않는다. `.pending`은 실패가 아니다(Ask to Buy). 복원은 sync가 던져도 엔타이틀먼트를 재조회한다 — 로컬에 이미 있는데 네트워크 실패로 "없음"이라 답하면 유료 사용자에게 거짓말이 된다.

### D-043. StoreKit Configuration 스킴 연결은 자동 검증이 불가능하다
- **시도.** 시뮬레이터에 설치 후 `xcrun simctl launch --console-pty`로 제품 조회를 프로브했다. 결과는 `productNotFound`.
- **그런데 이 결과는 결론이 아니다.** `simctl launch`는 **Xcode 스킴을 거치지 않으므로** StoreKit Configuration이 애초에 주입되지 않는다. 실패가 예상된 결과이고, 스킴 경로가 맞는지는 말해주지 않는다.
- **상태.** XcodeGen이 `storeKitConfiguration: JuiceLab.storekit`을 `identifier="../../JuiceLab.storekit"`로 적는데, 이 경로가 Xcode에서 해석되는지 **확인하지 못했다.**
- **J가 할 확인.** Xcode → Edit Scheme → Run → Options → StoreKit Configuration 드롭다운. 비어 있으면 직접 선택한다.
- **기록 이유.** "빌드 통과"를 "동작 확인"으로 보고하지 않기 위해(D-025). 검증 시도가 실패한 것과 검증이 불가능한 것은 다르고, 여기는 후자다.

---

## 2026-09-14 · S1.6 AdMob

### D-044. AdMob SDK를 별도 타겟 `CoreKitAdsGoogle`로 격리한다
- **결정.** SDK 의존성을 `CoreKitServices`가 아니라 새 타겟에만 건다.
- **이유.** GoogleMobileAds는 iOS 전용 바이너리 프레임워크다. `CoreKitServices`에 직접 걸면 macOS 호스트에서 `swift test`가 링크에 실패하고, 우리 개발 루프의 핵심인 밀리초 단위 테스트가 사라진다.
- **검증.** SDK 추가 후에도 `swift test` 117종이 0.037초에 돈다. `swift test`는 테스트 타겟이 의존하는 것만 빌드하므로 SDK 타겟은 아예 컴파일되지 않는다.
- **버전.** 13.9.0. **v12부터 Swift에서 `GAD` 접두사가 사라졌다** — `BannerView`, `InterstitialAd`, `MobileAds.shared`. 구버전 예제 코드를 그대로 쓰면 컴파일되지 않는다.
- **API 확인 방법.** 추측하지 않고 xcframework의 헤더에서 `NS_SWIFT_NAME`을 직접 읽었다. `currentOrientationAnchoredAdaptiveBanner(width:)`는 13.x에서 deprecated이고 `largeAnchoredAdaptiveBanner(width:)`가 대체다.

### D-045. 배너는 Coordinator가 소유한 단일 인스턴스를 재사용한다
- **문제.** SwiftUI는 부모 state가 바뀔 때마다 `updateUIView`를 호출한다. 게임에서는 타이머 한 틱, 점수 한 번 오를 때마다다. `updateUIView`에서 `BannerView`를 만들면 배너가 계속 리로드되어 깜빡이고 노출이 낭비되며 AdMob이 비정상 트래픽으로 볼 수 있다.
- **대응.** `Coordinator.init`에서 `BannerView`를 한 번 만들고 평생 재사용한다. `updateUIView`는 너비만 조정하고, **1pt 넘게 실제로 바뀐 경우에만** 다시 로드한다.
- **높이 선확보.** `AdBannerSlot`이 광고 도착 전에도 높이를 잡는다. 첫 노출에 화면이 밀리면 사용자가 누르려던 것을 잘못 누르고, 그게 Guideline 2.3.1 위반이다.
- **검증 방법.** JuiceLab의 Ads 탭이 250ms마다 부모를 재렌더한다. 그동안 배너가 깜빡이면 수정이 실패한 것이다.

### D-046. 광고 빈도 정책은 의도적으로 적게 보여준다
- **기본값.** 초반 3판 무광고, 전면 최소 간격 120초, 전면 사이 최소 2판, 보상형 후 45초 정숙.
- **이유.** 캐주얼 퍼즐 세션은 짧다. 매 판 전면은 2일차 리텐션을 깎는 가장 빠른 방법이고, **잃는 리텐션이 버는 노출보다 비싸다.**
- **보상형은 페이싱하지 않는다.** 사용자가 직접 요청한 것이라 거절하면 스스로 벌기로 한 힌트를 뺏는 것이다. 광고를 제거한 사용자는 **광고 없이 보상만 받는다** — 결제한 사람을 벌하지 않는다.
- **`InterstitialVerdict`를 남긴 이유.** "광고가 안 나온다"는 다른 방법으로 디버깅이 매우 어렵다. 실제 플레이어의 verdict 분포가 정책이 과한지 모자란지 알려준다.

### D-047. 실패한 노출은 기록하지 않는다
- **결정.** `showInterstitial()`이 false를 돌려주면 `lastInterstitialAt`을 갱신하지 않는다.
- **이유.** 로드 실패를 노출로 세면 이후 정당한 기회 몇 번이 이유 없이 억제된다. 테스트로 고정했다.
- **continuation 안전성.** `GoogleAdPresenter`는 dismiss와 present 실패 양쪽을 한 곳(`finishPresentation`)으로 모은다. continuation을 두 번 resume하면 트랩이고, 한 번도 안 하면 호출자가 영원히 멈춘다.

---

## 2026-09-15 · S1.7 Analytics

### D-048. Firebase는 CoreKit의 타겟이 아니라 별도 패키지다
- **측정.** `firebase-ios-sdk` resolve에 **101초, checkouts 144MB**가 들었다 (gRPC 바이너리 아티팩트 포함).
- **결정적 사실.** SwiftPM은 **빌드하는 타겟과 무관하게 패키지가 선언한 모든 의존성을 resolve한다.** 즉 `CoreKit`에 Firebase를 넣으면 JuiceLab도, `swift test` 루프도, Firebase를 전혀 안 쓰는 도구도 전부 이 비용을 문다. AdMob 때처럼 타겟만 분리하는 것으로는 해결되지 않는다.
- **대응.** `Packages/CoreKitFirebase`를 독립 패키지로 만든다. 프로토콜과 이벤트 스키마는 `CoreKitServices`에 남으므로 게임은 Firebase 없이도 로깅 코드를 쓸 수 있다.
- **부수 이득.** 분석 백엔드는 이 포트폴리오에서 가장 교체 가능성이 높은 조각이다. 바뀌면 이 패키지만 갈아끼운다.
- **`verify.sh`.** 기본 3단계는 빠르게 유지하고 Firebase는 `VERIFY_FIREBASE=1`일 때만 빌드한다.

### D-049. 이벤트는 생성 시점에 정규화한다
- **문제.** 분석 백엔드는 잘못된 이벤트 이름을 **에러 없이 버린다.** 몇 주 뒤 대시보드에 데이터가 없는 것으로만 알게 되고, 그 데이터는 어느 게임에 더 투자할지 정하는 근거다.
- **대응.** `AnalyticsEvent` 생성자가 소문자화·치환·예약 접두사 제거·길이 제한·파라미터 수 제한을 수행한다.
- **정렬 후 자르는 이유.** 파라미터가 25개를 넘을 때 딕셔너리 순서로 자르면 **같은 호출부가 실행마다 다른 이벤트를 만든다.** 키를 정렬해서 결정론적으로 만들었고 테스트로 고정했다.
- **가장 중요한 테스트.** `GameEvent`의 모든 이름이 정규화를 통과해도 변하지 않는지 검사한다. 코드에 쓴 이름과 대시보드 이름이 달라지는 사고를 막는다.

### D-050. `Analytics`를 `AnalyticsHub`로 이름을 바꿨다
- **이유.** `FirebaseAnalytics.Analytics`와 충돌한다. 어댑터 안에서는 정규화해서 피할 수 있지만, 두 모듈을 같이 import하는 파일이 생기면 모호해진다.
- **시점.** 게임 코드에 퍼지기 전인 지금이 가장 싸다.

### D-051. `ad_suppressed`와 `stage_abandon`이 이 스키마의 핵심이다
- **`ad_suppressed(verdict:)`.** 광고 노출 수만 봐서는 정책이 과한지 모자란지 절대 알 수 없다. verdict 분포가 그걸 알려준다 — `onboardingGrace`가 대부분이면 유예가 길고, `tooSoon`이 대부분이면 간격이 과하다.
- **`stage_abandon(progress_pct:)`.** 이탈률만으로는 처방이 안 나온다. 진행률이 낮으면 "이해 실패", 높으면 "거의 다 왔는데 실패"이고 대응이 정반대다.
- **`share_trigger`.** 숏폼 영상은 리텐션·ASO와 나란한 세 번째 성장 레버인데 로깅하지 않으면 존재 자체가 안 보인다.

### D-052. 재참여 알림은 로컬 알림으로 공용화하고 FCM은 보류한다
- **결정.** `CoreKit`에 로컬 알림 스케줄러를 둔다. FCM(푸시)은 넣지 않는다.
- **공용/개별 경계.** 공용은 권한 요청 타이밍·스케줄 규칙·64개 제한 관리·문구 로테이션·설정 토글. 게임별은 문구 풀(`.xcstrings`)과 알림음뿐이다.
- **FCM을 미루는 이유.** 게임마다 Firebase 프로젝트 + APNs 키 + 원격 알림 entitlement + 콘솔 캠페인 운영이 붙는데, 얻는 것은 "예약 없이 아무 때나 보낼 수 있다" 하나다. 솔로 개발자가 10개 게임에 캠페인을 돌릴 일은 드물다.
- **전환 신호.** 특정 게임에 DAU가 붙어 "신규 스테이지 추가를 알리고 싶다"는 필요가 처음 생기면 그때 `CoreKitFirebase`에 어댑터를 추가한다.
- **설계에 반드시 반영할 제약 4가지.**
  1. **iOS는 앱당 대기 로컬 알림 64개가 하드 캡이다.** 롤링 윈도우로 관리해야 한다.
  2. **권한을 첫 실행에 묻지 않는다.** 맥락 없는 요청은 거절률이 높고, 한 번 거절당하면 앱 안에서 다시 물을 수 없다(설정 앱으로 보내야 하며 거기까지 가는 사용자는 거의 없다). 첫 클리어 직후에 묻는다.
  3. **조용 시간.** 타임존 이동·서머타임을 고려하지 않으면 새벽에 울리고 그게 삭제 사유가 된다.
  4. **Guideline 4.5.4.** 광고·프로모션 성격 알림은 리젝이다. 게임 상태 기반 문구만 쓰고 앱 내에서 끌 수 있어야 한다.

---

## 2026-09-15 · S1.8 Local Notifications

### D-053. 감쇠 사다리 — 매일 울리는 구조를 만들 수 없게 설계한다
- **결정.** 마지막 플레이로부터 1·3·7·14·30일, 한 달에 총 5회. 매번 현지 저녁 7시.
- **이유.** 매일 알림은 사용자를 "스와이프해서 지우는 습관"으로 훈련시키고 그다음이 앱 삭제다. **삭제 한 번이 그 알림들이 평생 벌 노출보다 비싸다.** 빈도를 설정값이 아니라 구조로 못 박았다.
- **핵심 동작.** `refresh()`는 매번 전부 취소하고 새로 짠다. 그래서 **돌아온 플레이어는 옛 계획에 쫓기지 않는다.** 테스트로 고정했다.
- **64개 제한.** 동시 대기가 5~6개라 애초에 닿지 않는다. 제한은 "매일 반복"을 예약할 때만 문제가 되고, 그 설계를 택하지 않은 것이 곧 해결이다. `maxPending`으로 방어적으로도 막았다.

### D-054. 프로모션 테마를 타입 수준에서 없앴다
- **결정.** `NotificationTheme`은 `progress` / `curiosity` / `lossAversion` 3종뿐이다.
- **이유.** Guideline 4.5.4는 광고성 알림을 리젝한다. "광고 제거 할인!" 같은 문구를 **쓸 수 있는 API가 없는 편이** 문서로 금지하는 것보다 안전하다.
- **`lossAversion`은 조건부다.** 스트릭이 있는 게임에서만 생성되고, 없으면 자동으로 빠진다.

### D-055. 권한 요청은 앱 생애에 사실상 한 번뿐이다
- **결정.** `requestAuthorizationAfterFirstClear()` — 첫 클리어 전에는 물어볼 수 없고, 한 번 물으면 다시 묻지 않는다.
- **이유.** 맥락 없는 첫 실행 요청은 대부분 거절되고, **거절당하면 앱 안에서 재요청이 불가능하다.** 유일한 경로가 설정 앱이고 거기까지 가는 사용자는 거의 없다. 그래서 요청 기회를 "다시 오고 싶은 이유가 생긴 시점"에 쓴다.

### D-056. 캘린더 트리거가 아니라 interval 트리거를 쓴다
- **결정.** `UNTimeIntervalNotificationTrigger`.
- **이유.** 캘린더 트리거는 시각 성분을 보관하므로 **비행기에서 타임존이 바뀌면 재해석되어 새벽에 울릴 수 있다.** 계획 단계에서 정확한 순간을 이미 확정했으므로 경과 시간으로 예약한다. 타임존 변경은 다음 앱 실행의 `refresh()`가 바로잡는다.
- **같이 막은 것.** 과거 시각 예약. 과거 트리거는 등록 즉시 발사되어 버그처럼 보이고 플레이어의 관용을 한 번에 태운다. 테스트로 고정했다.

### D-057. 조용 시간에 걸린 스트릭 경고는 포기한다
- **결정.** 스트릭 만료 경고가 조용 시간에 떨어지면 아예 보내지 않는다. 시간을 당기거나 미루지 않는다.
- **이유.** 당기면 "아직 시간이 많이 남았는데?"가 되어 무의미하고, 미루면 이미 끊긴 뒤다. **새벽 4시에 깨우는 것보다 스트릭을 조용히 잃는 편이 낫다.**

---

## 2026-09-15 · S1.9 Localization & Theme

### D-058. `CoreKitUI` 타겟을 신설한다
- **결정.** 테마와 공통 문자열을 담는 SwiftUI 전용 타겟. `CoreKitJuice`에 의존한다.
- **이유.** 테마는 juice도 services도 아니다. Phase 2의 공통 화면(Home/Result/Settings)이 들어올 자리이기도 하다. 외부 의존이 없어 프리뷰가 빠르다.

### D-059. 테마는 색만이 아니라 폰트 패밀리와 모서리까지 포함한다
- **결정.** `GameTheme` = `ThemePalette` + `ThemeTypography`(패밀리 포함) + `ThemeMetrics`(모서리·간격·그림자).
- **이유.** Guideline 4.3(템플릿 스팸) 대응이 이 타입의 존재 이유다. **색만 바꾸면 10개 앱이 여전히 같은 앱으로 보인다.** 심사자는 나란히 놓고 본다. 모서리 반경은 바꾸기 가장 싸면서 체감 차이는 색보다 크다.
- **`placeholder`에 대한 경고.** 중립 기본값을 그대로 출시하는 것이 곧 그 문제다. 문서에 명시했다.

### D-060. String Catalog는 `swift build`에서 컴파일되지 않는다
- **발견.** `.xcstrings`는 SwiftPM CLI 빌드에서 **원본 그대로 번들에 복사만 된다.** 카탈로그 컴파일러는 Xcode 빌드 단계라 `xcodebuild`에서만 `en.lproj/Common.strings`가 생성된다. 실제로 양쪽 번들을 열어 확인했다.
- **결과.** 호스트(`swift test`)에서는 모든 키가 자기 자신으로 해석된다. 런타임 해석을 단언하는 테스트는 번역이 아니라 빌드 시스템을 검사하게 된다.
- **대응.** 테스트를 **카탈로그 JSON 데이터 검증**으로 바꿨다 — 키 존재, 출시 언어 번역 존재, 고아 키 없음. 실제 버그(키·번역 누락)를 잡으면서 호스트에서 돈다.
- **같이 발견한 함정.** `Package.swift`에 `defaultLocalization`이 없으면 **로컬라이즈 리소스가 아예 처리되지 않는다.** 빌드는 성공하고 런타임에 키가 노출되는 것으로만 알게 된다.

### D-061. 공통 문자열을 게임마다 번역하지 않는다
- **결정.** `CommonStrings` 26종을 `CoreKitUI`가 소유한다. 게임은 고유 문자열만 자체 카탈로그에 둔다.
- **이유.** "구매 복원"이 10개 앱에서 3가지로 번역되는 것을 막는다. 더 중요하게는 **번역 누락이 10번이 아니라 1번만 생긴다.**
- **고아 키 테스트를 넣은 이유.** 참조되지 않는 항목은 번역가에게 돈을 내고 번역시키는 죽은 무게다.

---

## 2026-09-15 · Phase 1 감사 (모델 전환 전)

구현 전체를 다시 읽어 결함 7건을 찾아 고쳤다. 전부 **테스트가 닿지 않는 플랫폼 계층**에 있었다.
순수 로직은 171개 테스트가 이미 지키고 있었고, 새로 나온 문제는 하나도 그쪽이 아니었다.

### B-01. 톤 버퍼 캐시 키에 음계와 루트가 빠져 있었다 (심각)
- **증상.** `PitchedTonePlayer`의 캐시 키가 `(weight, stepIndex)`뿐이었다. 게임이 `scale`이나 `root`를 바꿔도 **이전 음계의 버퍼가 영구히 재생된다.** 소리는 나므로 실패로 보이지도 않는다.
- **도달 경로.** JuiceLab의 Audio 탭에 음계 피커가 있어 지금 당장 재현된다.
- **수정.** 키에 `scale`·`root` 포함. 회귀 테스트는 두 음계의 **런 전체**를 비교한다 — 개별 스텝은 반음이 우연히 같을 수 있다(step 3은 두 펜타토닉 모두 7반음).

### B-02. 배너 슬롯이 화면 너비로 높이를 계산했다
- **증상.** 배너는 `proxy.size.width`로 그리는데 예약 높이는 `UIScreen.main.bounds.width` 기준이었다. iPad Split View·Slide Over처럼 컨테이너가 화면보다 좁으면 **예약 공간과 실제 배너가 어긋나 잘리거나 빈틈이 생긴다.** `UIScreen.main`은 iOS 16+ deprecated이고 멀티 씬에서 의미가 없다.
- **수정.** `onGeometryChange`로 슬롯 자신의 너비를 재고 같은 값에서 높이를 파생. 너비를 알기 전에는 배너를 만들지 않아 **임시 너비로 한 번 로드했다가 다시 로드하는 낭비**도 없앴다.

### B-03. 승리 연출 완료가 렌더 루프에 묶여 있었다 (심각)
- **증상.** 완료를 `TimelineView` 안의 `onChange(of: elapsed >= total)`로 감지했다. 타임라인이 틱을 멈추면(뷰가 스크롤로 벗어남, 특정 타이밍의 백그라운드 전환) **완료가 보고되지 않고 게임이 승리 화면에 영구히 갇힌다.**
- **수정.** 완료를 타이머 `Task`가 구동한다. 렌더는 보이는 것만 담당한다. 외부에서 닫히거나 뷰가 사라지면 취소한다.

### B-04. 햅틱 엔진 설정이 주석과 반대였다
- **증상.** 주석은 "버스트 사이에 하드웨어를 따뜻하게 유지한다"인데 `isAutoShutdownEnabled = true`였다. 유휴 시 엔진이 내려가고 `stoppedHandler`가 nil로 만들어 **다음 탭마다 엔진을 새로 만든다.** 재생성 비용 때문에 버스트 첫 탭이 늦는데, 그건 상승 시퀀스가 의존하는 바로 그 지점이다.
- **수정.** `isAutoShutdownEnabled = false`. 수명은 `teardown()`이 관리하고, 백그라운드 전환은 어차피 엔진을 멈춘다.

### B-05. 알림 취소가 앱 전체를 지웠다
- **증상.** `removeAllPendingNotificationRequests()`는 앱 전역이다. 공용 라이브러리가 **게임이 자체 목적으로 건 알림까지 삭제한다.**
- **수정.** `corekit.reengagement.` 접두사를 소유권 표시로 두고 그것만 제거한다. 회귀 테스트가 모든 계획 항목의 접두사를 강제한다.
- **부수 수정.** 취소를 async로 바꿨다. 완료 핸들러로 두고 즉시 반환하면 **취소가 다음 예약 뒤에 도착해 방금 만든 알림을 지울 수 있다.**

### B-06. 콘솔 분석 리포터가 무한히 커졌다
- **증상.** 이벤트 배열에 상한이 없었다. 긴 세션은 수천 개를 쌓고, 출시 빌드에 등록되면 **분석 탓이라고 아무도 의심하지 않을 느린 누수**가 된다.
- **수정.** 기본 200개 상한, 오래된 것부터 버린다.

### B-07. 전면·보상형이 겹치면 continuation 두 개가 같은 결과로 해소됐다
- **증상.** `finishPresentation`이 두 continuation을 모두 해소한다. 두 광고가 동시에 대기 중이면 **보지도 않은 보상이 나가거나, 본 보상이 삼켜진다.**
- **수정.** `isPresenting` 가드로 전체 화면 광고를 한 번에 하나로 제한한다.

### 이번 감사에서 확인한 것
- 순수 로직 계층(레시피·정책·플래너·병합 규칙)에서는 **새 결함이 나오지 않았다.** 순수/플랫폼 분리가 실제로 값을 했다.
- 결함 7건 중 4건(B-01·B-03·B-04·B-07)은 **증상이 "조용한 오작동"** 이라 기기에서도 발견이 늦었을 것들이다.

---

## 2026-09-15 · Phase 1 2차 감사

1차 수정분을 포함해 전체를 다시 읽었다. **결함 6건 추가** (B-08~B-13).
1차 수정이 새 결함을 만들지는 않았고, 새로 나온 것은 전부 1차에서 덜 읽은 영역이었다.

### B-08. 톤 버퍼 캐시가 무한히 증가했다
- **증상.** `JuiceSequence.currentStep`은 상한 없이 오르는데 음정은 2옥타브에서 멈춘다. 캐시 키가 원시 스텝이라 **동일한 파형의 버퍼가 스텝마다 새로 만들어진다.** 버퍼 하나가 약 19KB이므로 긴 드래그 500스텝이면 10MB 가까이 쌓이고 `teardown()` 전에는 줄지 않는다.
- **수정 방식이 핵심.** 손으로 쓴 키를 고치는 대신 **캐시 키를 `ToneRecipe` 자체로 바꿨다.** 레시피는 파형을 결정하는 유일한 값이므로 키와 값이 어긋날 가능성이 구조적으로 사라진다. B-01도 이 변경으로 재발이 불가능해졌다.
- **회귀 테스트.** 상한 너머의 스텝들이 같은 레시피로 비교되는지, `milestone`·`error`가 스텝을 무시하는지.

### B-09. 광고 continuation이 영영 해소되지 않을 수 있었다 (심각)
- **증상.** SDK가 델리게이트를 끝내 호출하지 않으면 `withCheckedContinuation`이 resume되지 않는다. 크래시가 아니라 **반환하지 않는다** — 전면 광고를 기다린 뒤 다음 스테이지로 넘어가는 게임은 **그 자리에서 영구히 멈춘다.** 노출 하나를 놓치는 것과는 비교가 안 되는 실패다.
- **수정.** 90초 워치독이 미해소 상태를 "표시되지 않음"으로 마감한다. 뒤늦게 도착한 델리게이트는 이미 nil인 continuation을 보고 아무것도 하지 않는다.

### B-10. ATT 프롬프트를 앱이 active 되기 전에 요청했다
- **증상.** `requestTrackingAuthorization`은 앱이 활성 상태일 때만 프롬프트를 띄운다. 실행 직후 씬이 포그라운드로 오기 전에 호출하면 **아무것도 보여주지 않고 `.denied`를 반환한다.** 답은 되돌릴 수 없으므로 그 한 번의 타이밍 실수가 광고 단가를 영구히 깎는다.
- **수정.** `didBecomeActive`를 기다린 뒤 요청한다.

### B-11. 정규화가 비ASCII 문자를 통과시켰다
- **증상.** Swift의 `Character.isLetter`는 한글·가나·키릴에 대해 참이다. 그래서 `"스테이지클리어"` 같은 이벤트 이름이 정규화를 **통과한 뒤 백엔드에서 조용히 버려진다.** 정규화가 존재하는 이유가 바로 이 실패 모드인데 정작 그 경로에 구멍이 있었다.
- **수정.** `isASCII` 조건 추가. 회귀 테스트가 한국어·일본어·러시아어·이모지 입력을 검사한다.

### B-12. 하네스의 행 수 표시가 항상 0이었다
- **증상.** `(try? store?.…count) as? Int ?? 0` — `try?`가 옵셔널 체이닝 위에 겹쳐 `Int??`가 되고 `as? Int`가 항상 실패한다. **중복 병합 검증이 의존하는 유일한 숫자**가 늘 0으로 보였다.
- **의미.** 하네스 코드도 검증 도구인 이상 틀리면 검증이 거짓이 된다.

### B-13. Firebase 리포터의 불필요한 `@unchecked Sendable`
- 저장 프로퍼티가 없는 final 클래스라 `@unchecked`가 필요 없다. 탈출구를 남겨두면 나중에 상태가 추가될 때 컴파일러가 잡아주지 못한다. 제거했다.

### 2차 감사 총평
- **1차 수정 9건 중 새 결함을 만든 것은 없었다.** 다만 B-08은 1차 수정(B-01)이 **증상만 고치고 원인을 남긴** 사례였고, 근본 수정(레시피 키잉)으로 대체했다.
- 위험 패턴 일괄 스캔 결과 `try!`·`fatalError`·강제 언래핑·`DispatchQueue`·`assumeIsolated`가 소스에 전혀 없다.
- 남은 `@unchecked Sendable`은 `ConsoleAnalyticsReporter` 하나뿐이고 `NSLock`으로 보호된다.

---

## 2026-09-15 · Phase 1 3차 감사 (방법 변경)

1·2차는 둘 다 "읽기"였다. 같은 방법을 세 번째 쓰는 대신 **측정 가능한 방법 다섯 가지**로 바꿨다.
결함 2건 추가(B-14·B-15), 그중 하나는 **실행해 보지 않으면 절대 나올 수 없는 크래시**였다.

### 방법과 결과
| 방법 | 결과 |
|---|---|
| 코드 커버리지 실측 | 미실행 함수 52개 식별 → 테스트 26종 추가, **60.5% → 68.2%** |
| 경고를 에러로 승격 빌드 | 통과 |
| 정적 분석기 (`xcodebuild analyze`) | 통과 |
| 테스트 자체 감사 (항등식·단언 없음·상수 단언) | 단언 없는 테스트 1건 → 보강 |
| **시뮬레이터 실행 + 로그** | **크래시 1건 발견 (B-15)** |

### B-15. `INFOPLIST_KEY_GADApplicationIdentifier`가 조용히 버려져 앱이 실행 즉시 죽었다 (심각)
- **증상.** `GADInvalidInitializationException` — 광고 SDK가 앱 ID 없이 초기화되어 앱이 종료된다.
- **원인.** Xcode의 `GENERATE_INFOPLIST_FILE` + `INFOPLIST_KEY_*` 방식은 **Apple이 정한 키 목록에만** 적용된다. `NSUserTrackingUsageDescription`은 목록에 있어 들어갔지만 `GADApplicationIdentifier`는 없어서 **경고 한 줄 없이 버려졌다.**
- **왜 안 잡혔나.** 빌드 성공, 테스트 207개 통과, 정적 분석 통과, 경고 0. **산출물을 실행해 보기 전까지 모든 신호가 초록색이었다.**
- **범위.** `docs/architecture/admob-setup.md`가 이 방식을 안내하고 있었으므로 **게임 10개가 전부 같은 크래시를 물려받았을 것이다.**
- **수정.** 실제 `Info.plist` 파일로 전환(XcodeGen `info:` 선언). 문서에 함정을 명시했다.
- **재발 방지.** `verify.sh`가 **빌드 산출물의 plist에서 필수 키를 직접 확인**한다. 빌드 성공은 plist가 올바르다는 증거가 아니다.

### B-14. 분석 옵트아웃 주석과 동작이 어긋났다
- `isEnabled` 주석은 "플레이어가 거부하면 전부 끈다"였지만 크래시 리포팅은 계속 돌았다. B-04와 같은 부류(주석이 코드와 반대).
- **결정.** 둘을 분리한다 — `isEnabled`(행동 분석)와 `isCrashReportingEnabled`(진단). 분석 거부는 행동 데이터에 대한 선택이지 크래시 진단을 멈춰 달라는 요청이 아니고, 그렇게 처리하면 거부한 사용자를 영영 지원할 수 없다. 한 스위치로 묶고 싶은 게임은 둘 다 설정하면 된다.

### 커버리지가 말해주는 것
0% 파일은 전부 기기에서만 실행되는 계층이다 — CoreHaptics, AVAudioSession, AVAudioEngine,
UNUserNotificationCenter, StoreKit, SwiftUI 뷰.
**3차에 걸쳐 찾은 결함 15건이 정확히 이 영역에 몰려 있다.** 순수 로직 계층에서는 세 번 모두 새 결함이 0건이었다.
남은 위험은 "더 읽어서" 줄일 수 있는 성질이 아니라, **실기기에서 실행해야만** 줄어든다.
